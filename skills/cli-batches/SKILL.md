---
name: cli-batches
description: Submit and monitor batch inference jobs from the CLI. Upload and manage files for batch processing, retrieve results, and integrate batch pipelines with CI/CD workflows.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Submit and monitor batch inference jobs from the CLI. Upload and manage files for batch processing, retrieve results, and integrate batch pipelines with CI/CD workflows.

## Quick install

```bash
npm install -g agentproxy   # or: npx agentproxy
agentproxy --version
```

## Subcommands

### `batches`

**Example:**

```bash
agentproxy batches
```

### `batches list`

**Flags:**

- `--status <s>`
- `--limit <n>`

**Example:**

```bash
agentproxy batches list
```

### `batches get <batchId>`

**Example:**

```bash
agentproxy batches get <batchId>
```

### `batches create`

**Flags:**

- `--endpoint <e>`
- `--completion-window <w>`
- `--metadata <kv>`

**Example:**

```bash
agentproxy batches create
```

### `batches submit`

**Flags:**

- `--endpoint <e>`
- `--wait`

**Example:**

```bash
agentproxy batches submit
```

### `batches cancel <batchId>`

**Flags:**

- `--yes`

**Example:**

```bash
agentproxy batches cancel <batchId>
```

### `batches wait <batchId>`

**Flags:**

- `--timeout <ms>`

**Example:**

```bash
agentproxy batches wait <batchId>
```

### `batches output <batchId>`

**Flags:**

- `--out <path>`

**Example:**

```bash
agentproxy batches output <batchId>
```

### `batches errors <batchId>`

**Flags:**

- `--out <path>`

**Example:**

```bash
agentproxy batches errors <batchId>
```

### `files`

**Example:**

```bash
agentproxy files
```

### `files list`

**Flags:**

- `--purpose <p>`
- `--limit <n>`

**Example:**

```bash
agentproxy files list
```

### `files get <fileId>`

**Example:**

```bash
agentproxy files get <fileId>
```

### `files upload <path>`

**Example:**

```bash
agentproxy files upload <path>
```

### `files content <fileId>`

**Flags:**

- `--out <path>`

**Example:**

```bash
agentproxy files content <fileId>
```

### `files delete <fileId>`

**Flags:**

- `--yes`

**Example:**

```bash
agentproxy files delete <fileId>
```
