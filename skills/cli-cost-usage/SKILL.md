---
name: cli-cost-usage
description: View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.

## Quick install

```bash
npm install -g agentproxy   # or: npx agentproxy
agentproxy --version
```

## Subcommands

### `cost`

**Flags:**

- `--period <range>`
- `--since <date>`
- `--until <date>`
- `--group-by <field>`
- `--api-key <key>`
- `--limit <n>`

**Example:**

```bash
agentproxy cost
```

### `pricing`

**Example:**

```bash
agentproxy pricing
```

### `pricing sync`

**Flags:**

- `--provider <p>`
- `--force`

**Example:**

```bash
agentproxy pricing sync
```

### `pricing list`

**Flags:**

- `--provider <p>`
- `--model <m>`
- `--limit <n>`

**Example:**

```bash
agentproxy pricing list
```

### `pricing get <model>`

**Example:**

```bash
agentproxy pricing get <model>
```

### `pricing defaults`

**Example:**

```bash
agentproxy pricing defaults
```

### `pricing show`

**Example:**

```bash
agentproxy pricing show
```

### `pricing set`

**Flags:**

- `--input <p>`
- `--output <p>`
- `--cache-read <p>`
- `--cache-write <p>`

**Example:**

```bash
agentproxy pricing set
```

### `pricing diff`

**Flags:**

- `--model <m>`

**Example:**

```bash
agentproxy pricing diff
```

### `usage`

**Example:**

```bash
agentproxy usage
```

### `usage analytics`

**Flags:**

- `--period <range>`
- `--provider <id>`

**Example:**

```bash
agentproxy usage analytics
```

### `usage budget`

**Example:**

```bash
agentproxy usage budget
```

### `usage list`

**Example:**

```bash
agentproxy usage list
```

### `usage get [scope]`

**Example:**

```bash
agentproxy usage get [scope]
```

### `usage set <amount>`

**Flags:**

- `--scope <s>`
- `--period <p>`

**Example:**

```bash
agentproxy usage set <amount>
```

### `usage reset [scope]`

**Example:**

```bash
agentproxy usage reset [scope]
```

### `usage quota`

**Flags:**

- `--provider <id>`
- `--check`

**Example:**

```bash
agentproxy usage quota
```

### `usage logs`

**Flags:**

- `--limit <n>`
- `--search <q>`
- `--since <ts>`
- `--follow`
- `--api-key <k>`

**Example:**

```bash
agentproxy usage logs
```

### `usage utilization`

**Flags:**

- `--api-key <k>`

**Example:**

```bash
agentproxy usage utilization
```

### `usage history`

**Flags:**

- `--limit <n>`

**Example:**

```bash
agentproxy usage history
```

### `usage proxy-logs`

**Flags:**

- `--limit <n>`

**Example:**

```bash
agentproxy usage proxy-logs
```
