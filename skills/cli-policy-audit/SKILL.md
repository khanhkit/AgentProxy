---
name: cli-policy-audit
description: Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.

## Quick install

```bash
npm install -g agentproxy   # or: npx agentproxy
agentproxy --version
```

## Subcommands

### `audit`

**Example:**

```bash
agentproxy audit
```

### `audit tail`

**Flags:**

- `--source <s>`
- `--follow`
- `--limit <n>`

**Example:**

```bash
agentproxy audit tail
```

### `audit search <query>`

**Flags:**

- `--source <s>`
- `--since <ts>`
- `--until <ts>`
- `--limit <n>`
- `--actor <id>`
- `--action <a>`

**Example:**

```bash
agentproxy audit search <query>
```

### `audit export <file>`

**Flags:**

- `--source <s>`
- `--format <f>`
- `--since <ts>`
- `--until <ts>`

**Example:**

```bash
agentproxy audit export <file>
```

### `audit stats`

**Flags:**

- `--source <s>`
- `--period <p>`

**Example:**

```bash
agentproxy audit stats
```

### `audit get <id>`

**Flags:**

- `--source <s>`

**Example:**

```bash
agentproxy audit get <id>
```

### `logs`

**Flags:**

- `--follow`
- `--filter <level>`
- `--lines <n>`
- `--timeout <ms>`
- `--base-url <url>`
- `--request-id <id>`
- `--api-key <key>`
- `--combo <name>`
- `--status <code>`
- `--duration-min <ms>`
- `--duration-max <ms>`
- `--export <path>`

**Example:**

```bash
agentproxy logs
```

### `policy`

**Example:**

```bash
agentproxy policy
```

### `policy list`

**Flags:**

- `--kind <k>`
- `--scope <s>`

**Example:**

```bash
agentproxy policy list
```

### `policy get <id>`

**Example:**

```bash
agentproxy policy get <id>
```

### `policy create`

**Example:**

```bash
agentproxy policy create
```

### `policy update <id>`

**Example:**

```bash
agentproxy policy update <id>
```

### `policy delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
agentproxy policy delete <id>
```

### `policy evaluate`

**Flags:**

- `--resource <r>`
- `--context <json>`

**Example:**

```bash
agentproxy policy evaluate
```

### `policy export <file>`

**Example:**

```bash
agentproxy policy export <file>
```

### `policy import <file>`

**Flags:**

- `--overwrite`

**Example:**

```bash
agentproxy policy import <file>
```

### `telemetry`

**Example:**

```bash
agentproxy telemetry
```

### `telemetry summary`

**Flags:**

- `--period <p>`
- `--compare-to <p>`

**Example:**

```bash
agentproxy telemetry summary
```

### `telemetry export`

**Flags:**

- `--out <path>`
- `--period <p>`

**Example:**

```bash
agentproxy telemetry export
```
